{
  "name": "my-resume",
  "version": "1.0.0",
  "description": "My personal resume",
  "scripts": {
    "build": "pandoc resume.md -o index.html --template template.html"  },
  "dependencies": {
    "pandoc": "0.2.0"
  }
}
